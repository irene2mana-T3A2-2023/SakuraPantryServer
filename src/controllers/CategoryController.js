// Get a list of all categories
export const getAllCategories = async () => {
  // eslint-disable-next-line no-console
  console.log('Get all Caterogies');
};

// Create a new category
export const createCategory = async () => {
  // eslint-disable-next-line no-console
  console.log('Create a new category');
};

// Update a specific category by slug
export const updateCategory = async () => {
  // eslint-disable-next-line no-console
  console.log('Get a specific Category by slug');
};

// Delete a specific category by slug
export const deleteCategory = async () => {
  // eslint-disable-next-line no-console
  console.log('Delete a specific product by slug');
};
